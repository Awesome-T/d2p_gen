name: exap
version: 0.0.1
descriptio: >-
  Eexample of using the package to generate 
  proto files `d2p_gen'.
publish_to: none
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  protobuf: ^3.0.0
  grpc: ^3.2.4
  d2p_annotation:
  d2p_gen:
     path: ../../d2p_gen
dev_dependencies:
  build_runner: ^2.3.3
  freezed: ^2.3.2
  freezed_annotation: ^2.4.1
  test:
